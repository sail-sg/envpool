diff --git a/src/engine/engine_io.c b/src/engine/engine_io.c
index c196ec1..0a72765 100644
--- a/src/engine/engine_io.c
+++ b/src/engine/engine_io.c
@@ -69,7 +69,7 @@ static const int ID = 54321;
 static int getnsize(void) {
   int cnt = 0;
 
-#define X(name) cnt += _Generic(MJMODEL_MEMBER(name), mjtSize: 1, default: 0);
+#define X(name) cnt++;
   MJMODEL_SIZES
 #undef X
 
diff --git a/src/xml/xml.cc b/src/xml/xml.cc
index ddb1db9..ddb5ef0 100644
--- a/src/xml/xml.cc
+++ b/src/xml/xml.cc
@@ -361,8 +361,8 @@ mjSpec* ParseXML(const char* filename, const mjVFS* vfs, char* error,
   int ndir = 0;
   mju_getResourceDir(resource, &dir, &ndir);
   std::string_view directory(dir, ndir);
 
-  mjSpec* spec = SpecFromXML({xml, xml + buffer_size}, directory,
-                             filename, vfs, error, nerror);
+  mjSpec* spec = SpecFromXML(std::string_view(xml, buffer_size), directory,
+                             filename, vfs, error, nerror);
 
   mju_closeResource(resource);
   return spec;
diff --git a/include/mujoco/mjplugin.h b/include/mujoco/mjplugin.h
--- a/include/mujoco/mjplugin.h
+++ b/include/mujoco/mjplugin.h
@@ -203 +203 @@
-    #pragma section(".CRT$XCU", read)
+    #pragma section(".CRT$XCV", read)
@@ -213,9 +213,9 @@
     #define mjPLUGIN_LIB_INIT(n)                                                          \
       static void __cdecl _mj_init_##n(void);                                             \
       /* use mjEXTERNC to prevent C++ name mangling */                                    \
-      /* allocate the function pointer to the .CRT$XCU section of the executable */       \
+      /* allocate the function pointer to the .CRT$XCV section of the executable */       \
       /* functions in this section are executed on startup before calling main() */       \
-      mjEXTERNC __declspec(allocate(".CRT$XCU"))                                          \
+      mjEXTERNC __declspec(allocate(".CRT$XCV"))                                          \
       void (__cdecl * _mj_ptr_##n)(void) = _mj_init_##n;                                  \
       /* Force the linker to include the pointer symbol */                                \
       __pragma(comment(linker, "/include:" LINKER_NAME #n))                               \
