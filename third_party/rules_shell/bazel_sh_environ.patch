--- shell/private/repositories/sh_config.bzl.orig
+++ shell/private/repositories/sh_config.bzl
@@ -88,6 +88,7 @@
 sh_config = repository_rule(
     environ = [
         "WINDIR",
+        "BAZEL_SH",
         "PATH",
     ],
     # TODO: Replace this with configure = True and add BAZEL_SH to the
